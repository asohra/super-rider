#[derive(Debug)]
pub struct Object {
    id: String,
}

// Load from penpot file.
pub fn load_penpot_file() {
    // Check if extracted directory exists.
    // Check if penpot file exists.
    // If penpot file exists and penportfile.lastmodified != extracteddirectory.lastmodified
    //      extract pentpotfile into extracteddirectory
    // Load objects from extracteddirectory.
}
